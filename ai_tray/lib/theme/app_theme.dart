/// Builds Material 3 themes from branded presets.
///
/// Canonical implementation: `ThemeFactory` in `theme_factory.dart`.
library;

export 'package:ai_tray/theme/theme_factory.dart' show AppTheme, ThemeFactory;
